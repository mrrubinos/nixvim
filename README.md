# nixvim

Personal Neovim configuration built with [nixvim](https://github.com/nix-community/nixvim).

## Dependencies

- **FiraCode Nerd Font Mono**: used as `guifont` for Neovide. Must be installed on the host system (e.g. via `nerd-fonts.fira-code` in NixOS/home-manager).
